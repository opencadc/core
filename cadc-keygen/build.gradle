plugins {
    id 'java'
    id 'application'
}

repositories {
    mavenCentral()
    mavenLocal()
}

sourceCompatibility = 11

group = 'org.opencadc'

version = '1.0'

mainClassName = 'ca.nrc.cadc.keygen.Main'

dependencies {
    implementation 'org.opencadc:cadc-util:[1.6,2.0)'
    
    testImplementation 'junit:junit:4.13'
}

configurations {
  runtimeOnly.exclude group: 'javax.servlet'
  runtimeOnly.exclude group: 'org.jdom'
  runtimeOnly.exclude group: 'org.json'
  runtimeOnly.exclude group: 'org.springframework'
  runtimeOnly.exclude group: 'xerces'
}
